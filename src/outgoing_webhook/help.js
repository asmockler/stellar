export function help(params) {
  return 'I\'m here to help you find information from previous Slack conversations. To use me, just type `tars {command} {params}` e.g. `tars search bananas`. I\'ll look around and get back to you as soon as I can with some helpful links!'
}
